export const mobileChartMessageTypes = {
  SET_CANDLE_TYPE: 'set_candle_type',
  SET_INTERVAL: 'set_interval',
  SET_RESOLUTION: 'set_resolution',
  SET_INSTRUMENT: 'set_instrument',
};

Object.freeze(mobileChartMessageTypes);

export default mobileChartMessageTypes;
