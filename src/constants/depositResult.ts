const DepositResult = {
    SuccessStatus: "success",
    FailStatus: "fail",
    params: {
        amount: "amount",
        status: "status"
    }
}

Object.freeze(DepositResult);
export default DepositResult;