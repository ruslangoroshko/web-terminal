const API_DEPOSIT = {
  CALLBACK: {
    EXACTLY: '/callback/exactly',
  },
  DEPOSIT: {
    CREATE: '/Create',
    GET_CRYPTO_WALLET: '/GetCryptoWallet',
  },
};

Object.freeze(API_DEPOSIT);

export default API_DEPOSIT;