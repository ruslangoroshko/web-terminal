import styled from '@emotion/styled';

const Badge = styled.span`
  background-color: #00ffdd;
  border-radius: 2px;
  padding: 1px 3px;
  color: #1A1E22;
  font-size: 9px;
  line-height: 1;
`;

export default Badge;
